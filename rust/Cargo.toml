[workspace]
resolver = "2"
members = ["bloomfs-block", "bloomfs-alloc", "bloomfs-inode", "bloomfs-dedup", "bloomfs-bloom", "bloomfs-dir", "bloomfs-cow", "bloomfs-store", "bloomfs-fs", "bloomfs-fuse", "bloomfs-cli"]

[workspace.package]
edition = "2021"
license = "MIT"
rust-version = "1.96"
