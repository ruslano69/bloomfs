[workspace]
resolver = "2"
members = ["bloomfs-block", "bloomfs-alloc", "bloomfs-inode", "bloomfs-dedup", "bloomfs-bloom", "bloomfs-dir"]

[workspace.package]
edition = "2021"
license = "MIT"
rust-version = "1.96"
