[package]
name = "bloomfs-alloc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "BloomFS free-space allocator: a first-fit contiguous cluster bitmap with deferred-free."

[dependencies]
