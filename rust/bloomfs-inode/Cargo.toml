[package]
name = "bloomfs-inode"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "BloomFS 128-byte inode (explicit little-endian on-disk encoding) and its CoW RAM table."

[dependencies]
bloomfs-block = { path = "../bloomfs-block" }
