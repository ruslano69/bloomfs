[package]
name = "bloomfs-dedup"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "BloomFS in-RAM deduplication table: content hash -> stored copy + reference count."

[dependencies]
